<h2>編集フォーム</h2>

<%= form_tag("/#{@task.id}/update") do%>
タスク名<br><input name="name" value="<%= "#{@task.name}" %>"><br>
タスクの説明<br><textarea name="description" rows="3" cols="80"><%= "#{@task.description}" %></textarea><br>

<input type="submit" value="保存">
<% end %>
